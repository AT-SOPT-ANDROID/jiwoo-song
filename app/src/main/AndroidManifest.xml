<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ATSOPTANDROID"
        tools:targetApi="31" >
        <activity
            android:name="org.sopt.at.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.ATSOPTANDROID" />
         <!-- <activity -->
        <!-- android:name=".ui.theme.ListActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name="org.sopt.atsoptandroid.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ATSOPTANDROID" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.sopt.atsoptandroid.SignupActivity"
            android:exported="true"
            android:label="회원가입"
            android:parentActivityName="org.sopt.atsoptandroid.LoginActivity"
            android:theme="@style/Theme.ATSOPTANDROID" />
        <activity
            android:name="org.sopt.atsoptandroid.MyActivity"
            android:exported="true"
            android:label="마이페이지"
            android:parentActivityName="org.sopt.atsoptandroid.LoginActivity"
            android:theme="@style/Theme.ATSOPTANDROID" />
    </application>

</manifest>